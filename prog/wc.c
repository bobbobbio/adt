#include <adt.h>
#include <stdtyp/argparse.h>

int
main(int argc, char **argv)
{
   /*
   create(argparse, ap);
   argparse_add_optional_arg(&ap, strw("bytes"), 'b', "print the byte counts");
   */

   return EXIT_SUCCESS;
}
