CFLAGS += -Wno-unused-function
CFLAGS += -Wall
CFLAGS += -Werror
CFLAGS += -D_C99_SOURCE
CFLAGS += -D_POSIX_C_SOURCE
CFLAGS += -std=c99
CC = gcc

!cc_prog = |> $(CC) $(CFLAGS) %f -o %o |>
!cc = |> $(CC) $(CFLAGS) -c %f -o %o |> %B.o
!ar = |> ar rcs %o %f |>

.gitignore
