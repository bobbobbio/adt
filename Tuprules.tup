CFLAGS += -D_POSIX_C_SOURCE
CFLAGS += -D_C99_SOURCE
CFLAGS += -Wall
CFLAGS += -Werror
CC = gcc

!cc_prog = |> $(CC) $(CFLAGS) %f -o %o |>
!cc = |> $(CC) $(CFLAGS) -c %f -o %o |> %B.o
!ar = |> ar rcs %o %f |>

.gitignore
